# 本地运行配置模板
#
# 用法：
#   cp .env.local.example .env.local
#   然后在 .env.local 里填入下面两项，再运行 node local-server.js
#
# 注意：请把真实值填在 .env.local（已被 .gitignore 忽略），不要提交到仓库。

# Cline 账号 refreshToken，运行 python cline_oauth.py 获取。
# 支持多账号：一行一个 token，额度用尽会自动切号。
CLINE_REFRESH_TOKEN=

# 客户端访问密钥，自己设一个（客户端请求时用 Authorization: Bearer <此值>）。
# ⚠️ 必填：留空则聊天端点会拒绝所有请求（v1.2.0 起不再回退到公开默认密钥）。
API_KEY=sk-cline-local
