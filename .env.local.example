# 本地运行配置模板
#
# 用法：一般不用手动复制 —— 首次运行 start.bat / start.sh / start.mjs 时，
#       如果 .env.local 不存在，会自动按本文件创建一份。
#       想手动来也行：
#         cp .env.local.example .env.local
#       然后运行 node local-server.js
#
# 注意：请把真实值填在 .env.local（已被 .gitignore 忽略），不要提交到仓库。

# Cline 账号 refreshToken，运行 python cline_oauth.py 获取（脚本会自动填到这里）。
# 也可以启动服务后在控制台「账号」页点「登录新账号」。
#
# ⚠️ 这里只支持一个账号：本文件是逐行解析 key=value 的，多行 token 会被忽略。
#    需要多账号（额度用完自动切号）请在控制台里逐个登录，
#    或用真多行的环境变量 CLINE_REFRESH_TOKEN（见 docs/常见问题.md）。
CLINE_REFRESH_TOKEN=

# 客户端访问密钥，客户端请求时用 Authorization: Bearer <此值>。
# 留空即可：首次启动会自动生成一个随机密钥并写回本文件，
# 同时注入控制台页面，所以本地使用无需手填。
# （想自己指定就填在这里；线上部署则必须在环境变量里设置，不能用这个文件。）
API_KEY=
